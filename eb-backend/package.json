{
  "name": "rest-express-backend",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node server/index.ts",
    "simple-dev": "ts-node server/simple-index.ts",
    "minimal-dev": "ts-node server/minimal-index.ts",
    "build": "esbuild server/minimal-index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node dist/minimal-index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "db:push": "drizzle-kit push",
    "db:migrate": "node migrate-wbs-dates.cjs"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "postgres": "3.4.5"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "20.16.11",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.1",
    "typescript": "5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
